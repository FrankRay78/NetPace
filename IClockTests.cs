//
// TestWeaver Generated Tests
//
// Auto-generated by DeeperCore TestWeaver
// File: IClockTests.cs
// Generated: 2025-12-01T09:50:19.746267
//
// These tests cover edge cases and boundary conditions discovered
// through TestWeaver's 5-phase analysis methodology.
//

using System;

public class IClockTests
{
[Fact]
public void IncrementingClockStub_Now_IncrementsBy5SecondsOnEachCall()
{
    // Arrange
    var clock = new IncrementingClockStub();
    var expectedStartDate = new DateTime(1980, 1, 1, 10, 5, 0);

    // Act & Assert
    for (int i = 0; i < 5; i++)
    {
        var currentTime = clock.Now;
        var expectedTime = expectedStartDate.AddSeconds(i * 5);

        Assert.Equal(expectedTime, currentTime,
            $"Call {i + 1}: Expected {expectedTime}, but got {currentTime}");

[Fact]
public void Now_ReturnsCurrentSystemTime()
{
    // Arrange
    var clock = new Clock();
    var beforeTest = DateTime.Now;

    // Act
    var result = clock.Now;
    var afterTest = DateTime.Now;

    // Assert
    Assert.InRange(result, beforeTest, afterTest);
    Assert.Equal(DateTimeKind.Local, result.Kind);

[Fact]
public void IncrementingClockStub_Now_InitialValueIsCorrect()
{
    // Arrange
    var clock = new IncrementingClockStub();
    var expectedInitialDate = new DateTime(1980, 1, 1, 10, 5, 0);

    // Act
    var initialNow = clock.Now;

    // Assert
    Assert.Equal(expectedInitialDate, initialNow);
    Assert.Equal(0, initialNow.Second, "The initial value should have 0 seconds");

[Fact]
public void IncrementingClockStub_Now_IncrementsByFiveSecondsOverMultipleCalls()
{
    // Arrange
    var clock = new IncrementingClockStub();
    var expectedStartDate = new DateTime(1980, 1, 1, 10, 5, 0);

    // Act & Assert
    for (int i = 0; i < 5; i++)
    {
        var currentTime = clock.Now;
        var expectedTime = expectedStartDate.AddSeconds(i * 5);

        Assert.Equal(expectedTime, currentTime, 
            $"Call {i + 1}: Expected {expectedTime}, but got {currentTime}");

[Fact]
public void ClockStub_Now_ReturnsFixedDate()
{
    // Arrange
    var clockStub = new ClockStub();
    var expectedDateTime = new DateTime(1980, 1, 1, 10, 5, 0);

    // Act
    var result = clockStub.Now;

    // Assert
    Assert.Equal(expectedDateTime, result);
    Assert.Equal(1980, result.Year);
    Assert.Equal(1, result.Month);
    Assert.Equal(1, result.Day);
    Assert.Equal(10, result.Hour);
    Assert.Equal(5, result.Minute);
    Assert.Equal(0, result.Second);

[Fact]
public void IncrementingClockStub_Now_StartsFromInitialDate()
{
    // Arrange
    var clockStub = new IncrementingClockStub();
    var expectedStartDate = new DateTime(1980, 1, 1, 10, 5, 0);

    // Act
    var actualStartDate = clockStub.Now;

    // Assert
    Assert.Equal(expectedStartDate, actualStartDate);
    Assert.Equal(expectedStartDate.Kind, actualStartDate.Kind, "DateTime kind should match");

[Fact]
public void IncrementingClockStub_Now_ReturnsCorrectInitialDateTime()
{
    // Arrange
    var incrementingClockStub = new IncrementingClockStub();
    var expectedInitialDateTime = new DateTime(1980, 1, 1, 10, 5, 0);

    // Act
    var result = incrementingClockStub.Now;

    // Assert
    Assert.Equal(expectedInitialDateTime, result);
    Assert.Equal(0, result.Millisecond, "The initial DateTime should have 0 milliseconds");

[Fact]
public void Now_ShouldHandleOverflowWhenCallCountReachesMaxValue()
{
    // Arrange
    var clock = new IncrementingClockStub();
    var expectedStartDate = new DateTime(1980, 1, 1, 10, 5, 0);
    var maxCalls = int.MaxValue;

    // Act
    for (int i = 0; i < maxCalls; i++)
    {
        clock.Now;

    var resultBeforeOverflow = clock.Now;
    var resultAfterOverflow = clock.Now;

    // Assert
    Assert.Equal(expectedStartDate.AddSeconds(maxCalls * 5), resultBeforeOverflow,
        "The last call before overflow should return the correct date and time.");
    Assert.Equal(expectedStartDate.AddSeconds((maxCalls + 1L) * 5), resultAfterOverflow,
        "The first call after overflow should continue incrementing correctly.");
}
