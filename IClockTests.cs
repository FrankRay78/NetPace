//
// TestWeaver Generated Tests
//
// Auto-generated by DeeperCore TestWeaver
// File: IClockTests.cs
// Generated: 2025-11-28T10:50:55.758891
//
// These tests cover edge cases and boundary conditions discovered
// through TestWeaver's 5-phase analysis methodology.
//

using System;

public class IClockTests
{
[Fact]
public void IncrementingClockStub_Now_IncrementsByFiveSecondsOverMultipleCalls()
{
    // Arrange
    var clock = new IncrementingClockStub();
    var expectedStartDate = new DateTime(1980, 1, 1, 10, 5, 0);

    // Act & Assert
    for (int i = 0; i < 5; i++)
    {
        var currentTime = clock.Now;
        var expectedTime = expectedStartDate.AddSeconds(i * 5);

        Assert.Equal(expectedTime, currentTime,
            $"Call {i + 1}: Expected {expectedTime}, but got {currentTime}");

[Fact]
public void IncrementingClockStub_Now_HandlesOverflow()
{
    // Arrange
    var clock = new IncrementingClockStub();
    var startDate = new DateTime(1980, 1, 1, 10, 5, 0);
    var maxValue = DateTime.MaxValue;
    long callsToOverflow = (maxValue - startDate).Ticks / (TimeSpan.TicksPerSecond * 5);

    // Act & Assert
    for (long i = 0; i < callsToOverflow + 1; i++)
    {
        var result = clock.Now;
        
        if (i < callsToOverflow)
        {
            Assert.True(result > startDate, $"Expected date to be greater than start date on call {i}");
            Assert.True(result <= maxValue, $"Expected date to not exceed MaxValue on call {i}");

        else
        {
            // On the last call, we expect it to overflow and start from the beginning
            Assert.Equal(startDate, result);

[Fact]
public void ClockStub_Now_ReturnsFixedDateTime()
{
    // Arrange
    var clockStub = new ClockStub();
    var expectedDateTime = new DateTime(1980, 1, 1, 10, 5, 0);

    // Act
    var result = clockStub.Now;

    // Assert
    Assert.Equal(expectedDateTime, result);
    Assert.Equal(1980, result.Year);
    Assert.Equal(1, result.Month);
    Assert.Equal(1, result.Day);
    Assert.Equal(10, result.Hour);
    Assert.Equal(5, result.Minute);
    Assert.Equal(0, result.Second);

[Fact]
public void Now_ReturnsIncrementedTimeOnEachCall()
{
    // Arrange
    var clock = new IncrementingClockStub();
    var expectedStartDate = new DateTime(1980, 1, 1, 10, 5, 0);

    // Act & Assert
    for (int i = 0; i < 3; i++)
    {
        var result = clock.Now;
        var expectedTime = expectedStartDate.AddSeconds(i * 5);

        Assert.Equal(expectedTime, result, 
            $"Call {i + 1}: Expected {expectedTime}, but got {result}");

[Fact]
public void Now_ShouldIncrementByFiveSecondsOnEachCall()
{
    // Arrange
    var clock = new IncrementingClockStub();
    var expectedStartDate = new DateTime(1980, 1, 1, 10, 5, 0);

    // Act & Assert
    for (int i = 0; i < 5; i++)
    {
        var currentTime = clock.Now;
        var expectedTime = expectedStartDate.AddSeconds(i * 5);

        Assert.Equal(expectedTime, currentTime, 
            $"Call {i + 1}: Expected {expectedTime}, but got {currentTime}");
}
