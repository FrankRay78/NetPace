# 287 — Check that a /speckit.reviewissue recommendation's Reason holds up against what it cites

| Finding | Level | Fix applied |
| --- | --- | --- |
| Reviewers (code-reviewer Important and comment-analyzer Important, independently): 9f793e0 said a Reason that fails its check "is recast: its Reason says it is your judgement", and never said to remove the failed claim — so the #270 Reason the issue exists to stop would have passed both the new paragraph and the self-check as `Reason: my judgement — Sonnet is the harness's standing default`. The same sentence also merged a claim found false with one the run merely cannot check, and the self-check clause omitted the cannot-check case. | Execution | A false claim is dropped and the Reason rewritten without it; an uncheckable claim survives only inside a judgement Reason; self-check mirrors both (858e629) |
| Reviewers (code-reviewer, comment-analyzer): 9f793e0 added a judgement option for Reasons without reconciling the two older rules that still demanded a cited Reason unconditionally — the quality bar ("The Reason cites the evidence that led you there") and step 6 ("Keep the Reason: cite tied to evidence"). A refine run following step 6 would have been pushed to keep or invent a citation, the exact failure the issue targets. | Execution | Both rules now allow a Reason marked as judgement (858e629) |
| Reviewer (code-reviewer): the new paragraph in 9f793e0 said "check every factual claim in every Reason" and named refine-run edits, with no limit to the re-framed gap — while step 6 requires untouched gaps byte-for-byte. The self-check sentence in the same commit carried the limit; the paragraph did not. `docs/study/286.md` records a clause in the same prompt being scoped against its own paragraph and not against step 6's refine-run rules one issue earlier — the second occurrence of that shape. | Execution | Paragraph scoped to the re-framed gap's Reason on a refine run (858e629); scoping every new self-check rule against step 6 flagged for `/capture-learnings` |
| Reviewer (comment-analyzer): the rule in 9f793e0 checks a claim "against the thing it names", but the #270 claim that motivated the issue — "X is the standing default" — names no source, so the rule gave no instruction for the one case it was written for. | Execution | Harness-behaviour claims are checked by searching for where the behaviour would be set, and finding nothing fails them (858e629) |
| Reviewer (comment-analyzer): step 3 and the comment template called every recommendation "your best judgement call", while 9f793e0 made `my judgement` the marker for a Reason with no evidence — one word carrying two meanings, inviting every Reason to open with the label and skip the check. | Execution | "judgement call" reworded to "best call" at step 3 and in the template (858e629) |
| Reviewers (code-reviewer, comment-analyzer): the CIR added in ea2422c asserted "no file in the repository naming Sonnet" — made false by that CIR itself, since `git grep -il sonnet HEAD` matched it — in a record about claims that must survive being checked. | Execution | Pinned to "no file on `main` named Sonnet at the time" (858e629) |
| Reviewer (/review-slop): the CIR added in ea2422c rejected "batching every Reason into one separate verification call, which has nothing to batch once no separate call exists" — a circular rejection restating an option the issue's redirect had already removed. | Execution | Sentence removed (858e629) |
| Reviewer (code-reviewer): `speckit-reviewissue.yml` allows only `Read`, `Glob`, `Grep`, `Write`, `gh issue comment` and `gh issue edit`, and its snapshot covers only the issue under review — so a true Reason citing another issue or PR is uncheckable in the workflow and posts as judgement there, while a local run posts the same claim as evidence. | Codebase | Recorded as a constraint in the CIR (858e629); widening the allowlist to `gh issue view` left as an author decision in PR #289 |
