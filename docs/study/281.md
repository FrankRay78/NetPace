# 281 — Make a /speckit.reviewissue comment triageable, and stop the category sweep inflating it

| Finding | Level | Fix applied |
| --- | --- | --- |
| Reviewers (comment-analyzer, Blocker; code-reviewer, Important): the CIR's `184, 192, 196 and 317 words` in c5f4eed — the sole quantitative justification for choosing a 120-word bound — was wrong in the fourth figure. An ad-hoc `awk` counter had no terminator for the last block, so gap 4 was counted past its `> _Answer:_` slot to end-of-file, absorbing the `### Notes for SDD` heading and both Commentary bullets that follow it (94 words). Two independent recounts put it at 223; the bug reproduces exactly. The figure had already reached a permanent record and c5f4eed's immutable commit message, and it also misled pr-test-analyzer into an Important finding that the bound needed a tokenization rule because "two counters diverged 42%" — a divergence that was this counter's bug, not ambiguity in the stated method, which reproduced exactly on the other three gaps. | Execution | Corrected to 184/192/196/223 with the counting method stated, and the correction noted in the CIR (3bfda8c) |
| Reviewer (comment-analyzer, Important): `This order is load-bearing`, written into the new ordering rule, is false. `/speckit.confirmissue` routes each folded decision by the *heading* its gap sat under (its step 4), not by the sequence of the two sections — swap them and its output is byte-identical. The claim was asserted about a downstream command's behaviour without reading it, and Requirements-first already had two correct rationales in reach (requirements are probed first; #281 puts the groups' order out of scope), neither of which was cited. Labelling a false claim load-bearing invites a future editor to test it, find it fails, and relax the genuine constraint beside it. | Execution | Reattributed to the grouping rather than the sequence, in both the prompt and the CIR's constraints (3bfda8c) |
| Reviewer (comment-analyzer, Important): the failure mode justifying this change's central design decision cannot occur. The prompt and CIR decision 2 both stated that a table row imitating `**N. <title>**` `would be folded as a phantom decision the author never made`, but `/speckit.confirmissue` hard-stops in its step 2 whenever any parsed gap looks unanswered — so a row in gap shape either blocks the entire fold, or its block-end resolves to the first real gap's answer slot and corrupts the decisions that do land. The rule was right and the shape was right; only the stated mechanism was wrong, and the real one is a stronger argument for the rule. | Execution | Both files restated to the two mechanisms that can actually occur (3bfda8c) |
| Reviewers (code-reviewer and comment-analyzer, independently, Important): the new `120 words per gap` bound was stated as an unscoped absolute — `no gap may exceed it` — against step 6's pre-existing mandate that a re-framed hedging gap *gain* 2–4 options with trade-offs plus a worked example. Neither instruction referenced the other, so a refine run would have had to silently break one of them. #281's AC #5 is unscoped in the same way, so the ambiguity was inherited rather than invented. | Execution | Bound scoped to first composition, with the refine-run expansion explicitly exempt (3bfda8c) |
| Reviewer (code-reviewer, Important): the new refine rule kept the at-a-glance table's row in step with a re-framed gap but said nothing about the gap's own `_<kind>:_` consequence line. Updating the row and leaving the gap line stale is precisely the drift AC #10 exists to prevent — and the rule's own guarantee that "a row and its gap are unmistakably the same thing" would break on the consequence while the title still matched. | Execution | Both places a consequence is stated now move together; the consequent AC #3 ordering drift on a posted comment documented rather than left implicit (3bfda8c) |
| The first review draft composed under the new prompt breached the new prompt's own 120-word bound on 3 of 3 gaps (173/126/122) with the bound sitting in front of it, and was then hand-cut to fit — which pr-test-analyzer (Blocker) correctly identified as circular evidence, since it showed a compliant gap could be authored once the number was known rather than that the prompt produced one. A stated number with no instruction to count is not a constraint. | Execution | Added a pre-post self-check step to the command; a later blind trial under the amended prompt came in 0 of 5 over the bound (3bfda8c) |
| Reviewer (pr-test-analyzer, two Blockers): the RED/GREEN evidence first offered did not isolate the change. The "before" was the live #278 comment from 2026-09-07 — two merged PRs stale, and proving it on its face by emitting `### Notes for SDD` where main emits `### Commentary` — and it was a *different issue* from the "after", so nothing attributed the difference to the prompt rather than to the issues. The controlled run was cheap and available throughout, and #278's own gap 2 had already written down the obstacle and its workaround. | Execution | Replaced with a blind A/B: two clean-context agents, same issue (#242), one prompt variant each, neither told a comparison was happening — 6/6 gaps over the bound before, 0/5 after |
| Reviewers (code-reviewer and comment-analyzer, Important): the `no gaps at all — omit the table along with the groups` exception added to the table rules had nothing behind it. The template carries conditional-omit directives for Technical gaps and Commentary but none for Requirements gaps, and its intro sentence unconditionally promises points that would not exist — so two readers would act differently, and the exception made the intro over-promise by one more element than before. The gap was already known: it is gap 3 of #278's own review. | Execution | Exception dropped rather than half-implemented; the template's missing omit directives left to #278's successor, where the whole zero-gap path belongs (3bfda8c) |
| A subagent briefed for a read-only dry run mutated `src/`: to ground one gap it applied `required` to both result types and ran `dotnet build`, then reverted. The brief forbade posting to GitHub but never forbade writing to the working tree, and the omission was invisible until the agent disclosed it. Verified reverted — `git diff -- src/` empty, both result types unchanged. Had it not reverted, it would have dirtied the tree mid-`/verify`, whose step 0 refuses to start on one. | Execution | Revert verified against HEAD before continuing; the brief's omission left for `/capture-learnings` |
| Review B (@claude, post-merge): the at-a-glance table is Markdown with `\|`-delimited cells, and neither the template nor its rules say what to do when a gap's short title or compressed consequence text contains a literal `\|`. The whole table design was scrutinised against `/speckit.confirmissue`'s parser and this second, simpler way to break the same table was not considered. | Execution | None — the review landed after merge; a one-line escaping note is still owed |
