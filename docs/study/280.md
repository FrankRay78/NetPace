# 280 — Delete the review comment once /speckit.confirmissue has folded it into the issue body

| Finding | Level | Fix applied |
| --- | --- | --- |
| Reviewer (silent-failure-hunter, Blocker): step 7 in 55324ea deleted by `<comment-id>` "already resolved in step 1", but step 1 resolved it through `gh issue view --json comments`, which returns the GraphQL node id — `DELETE repos/.../issues/comments/<id>` takes the numeric REST id. Verified live: `IC_kwDONbNDk88AAAABTCYLgw` against `5572529027`. Every run would have 404'd, and the "never fatal" rule written into the same commit would have reported that permanent failure as transient bad luck indefinitely. | Execution | Step 1 resolves the comment over REST, and selects by `created_at` (30acffd) |
| Reviewer (silent-failure-hunter, Blocker; code-reviewer rated the same defect a Suggestion): the guard's `jq -e '[.labels[].name] \| index("ready")'` added in 55324ea exits 5 on a missing or null `labels` key, and inside an `if` errexit is suppressed — so a jq error was indistinguishable from "not ready" and fell through to posting a duplicate review over a confirmed issue. The failure direction was the exact harm the guard exists to prevent. | Execution | Label read moved live and assigned outside the `if`, so a gh or jq failure fails the job (30acffd) |
| Reviewer (silent-failure-hunter): the guard added in 55324ea read labels from the snapshot taken in the previous step while reading comments live, pairing a stale half with a fresh one. A local `/speckit.confirmissue` is a second writer to the same state, so an issue confirmed between the two reads showed neither marker and would have been reviewed over its own decisions — with the post-condition check still passing the run green. | Execution | Both markers now read live, in the same step (30acffd) |
| Reviewers (silent-failure-hunter Blocker and code-reviewer Important, independently): 55324ea gated the only irreversible action in the command — deleting the review — on step 6 having succeeded, while step 6 is explicitly never-fatal, leaving "did it?" to the model's reading of tool output. The precedent against this sat in a file the same commit edited: CIR `2026-09-07` decision 5 moved the workflow's guard into a `run:` step precisely because "a model can skip an instruction". The branch applied that reasoning to the guard and not to the irreversible deletion it introduced. | Execution | Step 7 reads the label and the body back from the issue and requires both before deleting (30acffd) |
| The issue's technical notes and its own acceptance criteria could not both hold. The notes specify mode detection keyed on "`## Confirmed decisions` (or `ready`)" — an OR — while AC #8 requires a reopen path that does not hand-edit the issue body, and the body section can only be cleared by hand-editing it. The OR had to be cut to the label alone before any of it could be built. | Plan-spec | Marker narrowed to the `ready` label; the resolution and both rejected alternatives recorded as CIR decision 1 (55324ea) |
| Reviewer (code-reviewer): "carry across by hand any decision the new review does not re-raise", written into step 4 in 55324ea, names no actor and no moment. Read as an instruction to the model it contradicts the "replace that section" rule two lines above; read as one to the author, the only workable moment is before a run they have not started yet. On the reopen path it silently drops every decision the new review does not re-raise, with the review those decisions came from already deleted. | Execution | Made the command's own job, performed before the rebuild (30acffd) |
| Reviewer (comment-analyzer, Critical): the supersede banner added to CIR `2026-09-07` in 55324ea ended "everything else in this record stands", while a behaviour clause a few lines below still asserted the superseded contract — "the issue is already done" on sentinel presence. The banner actively vouched for the one clause it most needed to retract. | Execution | Second clause annotated in place and the banner's enumeration widened (30acffd) |
| Reviewers (comment-analyzer, code-reviewer): `speckit.draftissue.md:37` ingests the `<!-- speckit:review -->` comment as brief material, and was missed when counting the consumers of the deleted comment. `docs/study/279.md` records the same file and the same line being missed by a review of the same section one issue earlier — the second occurrence of that shape, which `.claude/memory/feedback_audit_class_after_two_failures.md` says is the point to fix the class. | Execution | Recorded as a known residual in the new CIR (30acffd); enumerating sentinel consumers flagged for `/capture-learnings` |
| `/verify` runs its slop pass inside the review step and applies review fixes after it, so no edit made while fixing a finding is ever slop-reviewed. Concretely: the workflow guard's comment block was trimmed by the slop pass, then grew past its original size while the three reviewers' findings were applied, and had to be re-tightened by hand before the commit. | Execution | Re-tightened manually (30acffd); the ordering gap in `/verify` itself is unaddressed |
| `use the most recent one by ID` has been in step 1 since before this branch, and was benign there — a wrong pick meant folding stale answers, recoverable by re-running. The same line now selects which comment is destroyed, and the only id on that path is a base64 node id that does not order chronologically; a repository can also carry both the legacy `MDEyOklzc3VlQ29tbWVudA…` and modern `IC_kwDO…` forms, which do not interleave under any comparison. | Codebase | Selection moved to `created_at` as part of the REST fix (30acffd) |
