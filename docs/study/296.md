# 296 — Run every *.tests.sh shell matrix in CI

| Finding | Level | Fix applied |
| --- | --- | --- |
| `/build`'s RED step has no runnable tool when the gate being added *is* CI: the issue's evidence contract needs a pushed run, which `/build` is forbidden to produce | Execution | RED shown as a local stand-in of the job's own discovery and run steps; on-PR half deferred to the PR (cfbb889) |
| Neither `act` nor `actionlint` is installed, so `shell-tests.yml` reaches the PR validated only by a plain YAML parse, never by an Actions-aware linter | Environment | Accepted; YAML checked with `python3 -c 'import yaml'` (cfbb889) |
| Three compound Bash probe commands were refused as "multiple operations" / "cannot be statically analyzed", so the local RED probe could not be run inline | Environment | Probe written to `.claude/scratch/296-probe.sh` and run as a single file |
| Reviewer (silent-failure-hunter): the CIR reasoned itself into treating an empty `git ls-files` result as a legitimate pass, so a drifted naming convention would report green having run nothing | Execution | `discover` now exits 1 on a zero count; matrix job unconditional (1bca9bb) |
| Three reviewers: the AC "fails CI" was satisfied by a non-required check, and filename-derived matrix contexts left branch protection nothing stable to require | Plan-spec | Fixed-name `shell-tests` job added (1bca9bb); adding it to the ruleset left to a human |
| `chain.tests.sh` needs `gh` and GNU `timeout` via `chain.sh`'s preflight, invisible from the test file, so the documented bash/jq/git-only contract was wrong | Codebase | Dependency list corrected in the CIR and the workflow header (1bca9bb) |
| Reviewers (pr-test-analyzer, silent-failure-hunter): `${{ matrix.script }}` was interpolated unquoted into `run:`, executing a filename that is attacker-controlled on a fork PR | Execution | Passed via `env:` and invoked quoted, plus an exec-bit check (1bca9bb) |
| Reviewer (code-reviewer): the branch added a seventh workflow while leaving the exhaustive CI inventory table, in the same file it edited, at six rows | Execution | `shell-tests.yml` row added to the table (1bca9bb) |
| `git commit` with a heredoc message asked for a confirmation no non-interactive session can answer, stalling a `/verify` that is designed to run unattended | Environment | Message written to `.claude/scratch/` and committed with `-F` (1bca9bb) |
