# 296 — Run every *.tests.sh shell matrix in CI

| Finding | Level | Fix applied |
| --- | --- | --- |
| `/build`'s RED step has no runnable tool when the gate being added *is* CI: the issue's evidence contract needs a pushed run, which `/build` is forbidden to produce | Execution | RED shown as a local stand-in of the job's own discovery and run steps; on-PR half deferred to the PR (cfbb889) |
| Neither `act` nor `actionlint` is installed, so `shell-tests.yml` reaches the PR validated only by a plain YAML parse, never by an Actions-aware linter | Environment | Accepted; YAML checked with `python3 -c 'import yaml'` (cfbb889) |
| Three compound Bash probe commands were refused as "multiple operations" / "cannot be statically analyzed", so the local RED probe could not be run inline | Environment | Probe written to `.claude/scratch/296-probe.sh` and run as a single file |
