# 279 — Rename Notes for SDD to Commentary, and give the section a boundary and a filter

| Finding | Level | Fix applied |
| --- | --- | --- |
| Reviewer (comment-analyzer, Blocker): the example bullet added in 87d239f asserted "frontmatter in this repo is a single `description:` line" — true of 6 of the 31 files under `.claude/commands/` and `.claude/skills/`, and `speckit.reviewissue.md` itself carries none. The phrase was lifted unverified from the issue's own motivation text, into a command that posts publicly under a CI instruction to cite real conventions from this repository. | Execution | Replaced with an illustration that asserts nothing about this repo (6c5cbc3) |
| Reviewer (comment-analyzer): "hands nothing on to a later command", written into the section description in 87d239f, is false — `speckit.draftissue.md:37` ingests the whole `<!-- speckit:review -->` comment as brief material. The issue's motivation had checked `/speckit.specify` and `/speckit.confirmissue` and concluded nothing downstream reads the section; the absolute was asserted on that two-command sample. | Execution | Narrowed to the claim that was actually verified — no downstream command *parses* it (6c5cbc3) |
| All three reviewers: `- state how many gaps + how many notes were raised` survived the rename at line 277. The pre-change check grepped the exact phrase "Notes for SDD" rather than the concept, so a lowercase "notes" was invisible to it — the failure `.claude/memory/feedback_grep_after_simplifying.md` already warns about. | Execution | Renamed, and dropped the demand for a count of a section the new filter makes often-absent (6c5cbc3) |
| Reviewer (code-simplifier): the fix for "one rule stated twice in two wordings, which then drifted apart" itself introduced three such duplications — a second definition of the section at line 74, the no-answer property stated three times in one sentence, and an omit rule carried in both the description and the emitted template. | Execution | Trimmed each to a single statement (6c5cbc3) |
| Reviewer (code-reviewer): AC #5's boundary is not decidable as written — "a fact already true of the codebase" and "an obligation this issue would newly impose" both fire on a pre-existing convention that this issue is merely the first work to trigger. The wording came verbatim from the issue's Capability section. A point wrongly pushed out of commentary becomes a numbered gap, which must then manufacture a question and an answer for `/speckit.confirmissue` to fold into the issue body as a Confirmed decision. | Plan-spec | Added a clause resolving it by the source of the constraint (6c5cbc3) |
| Reviewer (comment-analyzer): the new filter and the new example bullet, added in the same commit, contradicted each other — the filter excluded a bullet restating "a documented convention" while the example admitted "local conventions that are silent when broken", and a documented-but-ungated convention satisfies both. | Execution | Re-cut the filter around what the implementer already holds, and the example around what no gate enforces (6c5cbc3) |
