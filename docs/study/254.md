# 254 — Comment and formatting residue left by Surface Transfer Failures (#222)

| Finding | Level | Fix applied |
| --- | --- | --- |
| Item 2's acceptance grep could not come out empty once the item was done correctly. It asks for zero matches of `NetPace's own health` and `never completed` under `src/`, but the same item requires the `IsOperationalFault` XML doc to stay unchanged, and that doc contains `NetPace's own health` (`SpeedTestCommand.cs:177`). An unrelated test comment also contains `never completed` (`NetPaceConsoleTests.Failures.cs:46`). The check was written against the six target lines without being run against the rest of the tree. | Plan-spec | Removed only the six named lines, left both leftover matches in place, and called out the conflict in the /build report (779e785) |
| After the first edit, the acceptance grep still found two of the six `SpeedTestCommand` comments (lines 147 and 154). A `replace_all` Edit matched the 24-space indentation used in the `--loop` and `--count` branches, but the single-run branch is nested one level shallower (20 spaces), so its copies were skipped without any error. A green build and test run would never have caught it, because the change is comment-only. | Execution | Removed the two missed lines with a second edit at the correct indentation, re-ran the grep and the full suite, then committed (779e785) |
| Reviewer (code-simplifier): item 1b prescribed the replacement sentence in `ProfileXmlDocTests.cs` as its own line of about 111 characters, so the scenario comment no longer wrapped at the roughly 100 columns the rest of the block uses. /build copied the prescribed layout exactly. | Plan-spec | Simplifier moved the sentence onto the end of the line above, as it sat before the change; suite re-run green and committed (0b3306a) |
| Reviewer (comment-analyzer): item 1 deleted the Windows-only `Should.Throw<IOException>` check, the only code that showed `XDocument.Load(string)` throwing depends on the platform, but did not scope the `LoadXmlDoc` XML doc (`ProfileXmlDocTests.cs:81`). That doc still says `XDocument.Load(string)` "throws instead" with no qualifier, which is only true on Windows. | Plan-spec | None; named as a deferred out-of-scope finding in the /verify report |
| Reviewer (pr-test-analyzer): the issue describes the Windows-only check in `ProfileXmlDocTests.cs` as residue from #222, but `git log -S` shows it arrived with #237 (bd47f2f, "Profile XmlDoc File Sharing"). The misattribution did not change what had to be deleted. | Plan-spec | None; noted in the /verify report |
| /verify's first suite run reported `BUILD_EXIT=1` without building anything: its output went to `.claude/scratch/`, which is gitignored and so does not exist in a freshly created worktree, and the shell redirect failed before `dotnet build` ran. | Environment | Created the directory with `mkdir -p` and re-ran the build and suite, green |
