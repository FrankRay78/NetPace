# 270 — Add scripts/chain.sh — run the SDLC command chain end to end for one issue

| Finding | Level | Fix applied |
| --- | --- | --- |
| Reviewers (code-reviewer Blocker, silent-failure-hunter Important, independently): the confirmed decision on #270 recorded in `research.md` R2 — any `https://github.com/…/pull/<n>` URL anywhere in `/raise-pr`'s report means success — cannot distinguish a pull request the run opened from one it merely quoted. `/raise-pr` alone of the five stages emitted no machine-readable verdict (step 10 said only "Print the PR URL"), and its commonest failure, a pull request already open for the branch, reports an error whose text contains a valid pull request URL. The chain would print `[5/5] raise-pr — ok` and announce that older pull request as the run's result having pushed nothing. | Plan-spec | `/raise-pr` now prints `RAISED pr=<url>` only for a pull request it opened and the chain gates on that line; R2 amended and marked as needing ratifying, since it overrides a confirmed decision (576f865) |
| Reviewers (code-reviewer, silent-failure-hunter): R2's decision to search the whole report for `FAILED reason=` stops a run over a quotation — reproduced against prose describing the chain. Stages 2 and 4 are `/study`, whose declared job is writing prose about what failed, so a study row describing the chain's own failure handling would abort a healthy run and attribute the failure to itself. | Plan-spec | Scan anchored to a line start, with a case proving a mid-sentence quotation does not stop the run (576f865) |
| Reviewer (silent-failure-hunter Blocker): `claude -p` reports API errors, interrupted runs and exhausted turn limits in `is_error` inside the JSON while still exiting 0 — confirmed against a live call, where `is_error`, `subtype`, `result` and `session_id` are all top-level fields — while `jq`'s own exit 5 on a non-JSON reply was muted by `2>/dev/null` and the raw reply died with the function's `local`. Both surfaced as `no recognisable verdict`, blaming the stage for a missing token when the stage had never run, with the one artefact that would have explained it already discarded. | Execution | Envelope parsed once and checked, `is_error` reported as itself, raw reply printed when it will not parse (576f865) |
| Reviewers (silent-failure-hunter, pr-test-analyzer and code-reviewer, independently): a reply carrying no `session_id` made `${resume:+--resume "$resume"}` expand to nothing, so a study pass ran in a fresh session with no memory of the stage it was studying and reported an honest `STUDIED issue=… rows=0`. FR-011 makes resuming a hard MUST, and the resulting green run was indistinguishable from a clean one. | Execution | The chain stops when a stage whose session a later pass needs returns no id (576f865) |
| Verified during `/verify`: unquoting `"$prompt"` — the exact regression `chain.sh`'s own comment records as making an unattended call stall on the terminal — left all 28 cases green. `pr-test-analyzer`'s mutation run found 5 of 12 injected regressions surviving, including dropping `--dangerously-skip-permissions`, dropping `</dev/null`, blanking the closing line and emptying the default model. The stub joined its arguments with spaces, so a lost quote could not be seen, and the harness applied its own `</dev/null` to the chain, structurally masking the loss of the inner redirect. | Execution | Stub logs arguments field-separated, the chain's stdin carries data so a lost redirect is observable, 14 cases added; all 11 mutations re-run afterwards are caught (576f865) |
| Reviewers (comment-analyzer Blocker, code-reviewer): `chain.tests.sh` pointed at "specs quickstart §4 and §5" — the only `specs/` reference outside `specs/` in the diff, against the standing project rule, and `/raise-pr` deletes that folder at merge. The scenario it deferred to, "A failed stage can be reopened", is the one scenario with no automated test, so after merge it would have had neither a test nor a documented procedure. | Execution | Procedure moved into `docs/agentic-workflow-NetPace.md` and the comment reworded to name the checks rather than their location (576f865) |
| Reviewers (code-reviewer, pr-test-analyzer): the branch shipped its only gate ungated — nothing under `.github/workflows/` referenced `chain.sh` or `chain.tests.sh`, and the harness is not a `dotnet test` project, so no CI job saw it. Constitution Principle I requires a tool that can run in CI be gated there rather than verified once by hand. | Execution | Added as a step to `dotnet.yml`, alongside the .NET suite (576f865) |
| Reviewer (silent-failure-hunter Important): `[ -n "$(git status --porcelain)" ]` tested only the output, so a repository git cannot read — exit 128 with empty stdout, confirmed directly — passed as a clean tree and was then refused by the branch check with an empty branch name, telling the invoker they were on the wrong branch when git could not run at all. | Execution | git's exit code tested on both calls, with a case run from outside a repository (576f865) |
| Reviewer (code-reviewer Important): `chain.tests.sh` used `wc -l` output as a bare number in both the stub's call counter and `calls()`. BSD `wc` right-pads its output, so on macOS every reply lookup would read `reply-       1.json`, every case would report `no recognisable verdict` and the whole matrix would fail — against Principle IV, on a repository whose constitution requires cross-platform support. | Execution | Counted with `grep -c ''` (576f865) |
| Reviewer (silent-failure-hunter Important): `timeout` signals only its direct child, so a stalled stage's own background work — a test run, a subagent, each under `--dangerously-skip-permissions` — survives the kill and can still be writing to the working tree while the closing message promises the branch is exactly as that stage left it. The stall case cannot catch this, because the stub `exec sleep`s and so is the only process. | Execution | Caveat documented in `chain.sh` and `docs/agentic-workflow-NetPace.md`; the process-group kill left as an author decision rather than changing kill behaviour under review (576f865) |
| Reviewer (`@claude` on PR #295, Important): `STAGE_SESSION` was assigned only after a stage's reply parsed as JSON, so a stage failing before that — a stall, a launch failure, a reply that is not JSON — left the previous stage's id in place and the closing line sent the invoker to an already-finished session. The third instance on this branch of the same class as the unanchored `FAILED reason=` scan and the missing `session_id`: a diagnostic pointer that reads plausibly while naming the wrong thing. Every pre-parse failure case in the matrix drove stage 1, where the unset global made the fallback right by accident. The reported blast radius was stages 2 through 5; it was in fact stages 3 and 5 only, since on the study passes the stale value is the session that pass resumed. | Execution | `STAGE_SESSION` set to the resumed id at the top of `run_stage`, so fresh stages fall back and the study passes keep what they resumed, with three cases covering a non-first stage failing that early (621a04d) |
