//
// TestWeaver Generated Tests
//
// Auto-generated by DeeperCore TestWeaver
// File: ProgramTests.cs
// Generated: 2025-11-28T08:30:50.936463
//
// These tests cover edge cases and boundary conditions discovered
// through TestWeaver's 5-phase analysis methodology.
//

using System;

public class ProgramTests
{
[Fact]
public void GetCommandApp_ReturnsConfiguredCommandApp()
{
    // Arrange
    var mockRegistrar = new Mock<ITypeRegistrar>();

    // Act
    var app = Program.GetCommandApp(mockRegistrar.Object);

    // Assert
    Assert.NotNull(app);
    Assert.IsType<CommandApp<SpeedTestCommand>>(app);
    
    var commandApp = app as CommandApp<SpeedTestCommand>;
    Assert.Equal(Program.Description, commandApp.Description);
    
    // Verify that the Configure method was called
    // Note: We can't directly verify the ConfigureAction was used, 
    // but we can check if the app was configured with the expected description
    Assert.Equal(Program.Description, commandApp.Description);

[Fact]
public void Main_WithTestArgument_UsesStubImplementations()
{
    // Arrange
    string[] args = new[] { "--test" };

    // Act
    int result = Program.Main(args);

    // Assert
    Assert.Equal(0, result); // Assuming successful execution returns 0
    
    // We can't directly assert the registrations, but we can check if the method completes without exceptions
    // and returns the expected result. The use of stubs is implied by the successful execution.
    
    // Note: This test is limited because we can't easily verify the internal registrations.
    // A more comprehensive test would require exposing some internal state or using a mocking framework.

[Fact]
public void Main_WithTestArgument_UsesStubImplementations()
{
    // Arrange
    string[] args = new[] { "--test" };

    // Act
    int result = Program.Main(args);

    // Assert
    Assert.Equal(0, result);
    // Note: We cannot directly assert the use of stub implementations
    // due to the private nature of the registrar and command app.
    // This test mainly ensures that the application runs without errors
    // when the --test argument is provided.

[Fact]
public void Main_WithTestArgument_RegistersStubImplementations()
{
    // Arrange
    string[] args = new[] { "--test" };

    // Act
    int result = Program.Main(args);

    // Assert
    Assert.Equal(0, result); // Assuming successful execution returns 0

    // Unfortunately, we can't directly assert the registration of stub implementations
    // as they are internal to the Main method. We would need to refactor the code
    // to expose the registrar or its contents for proper testing.
    // For now, we can only test that the method executes without throwing an exception
    // when the --test argument is provided.
}
