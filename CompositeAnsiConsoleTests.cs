//
// TestWeaver Generated Tests
//
// Auto-generated by DeeperCore TestWeaver
// File: CompositeAnsiConsoleTests.cs
// Generated: 2025-11-27T15:04:59.672388
//
// These tests cover edge cases and boundary conditions discovered
// through TestWeaver's 5-phase analysis methodology.
//

using System;
using Xunit;



using Xunit;
using System;

public class CompositeAnsiConsoleTests
{
    [Fact]
    public void Constructor_ThrowsArgumentNullException_WhenPrimaryConsoleIsNull()
    {
        // Arrange
        IAnsiConsole primaryConsole = null;
        IAnsiConsole[] additionalConsoles = new IAnsiConsole[0];

        // Act & Assert
        Assert.Throws<ArgumentNullException>(() => new CompositeAnsiConsole(primaryConsole, additionalConsoles));
    }
}

# Import: CompositeAnsiConsole

using Xunit;
using Spectre.Console;

public class CompositeAnsiConsoleTests
{
    [Fact]
    public void Constructor_WithNullSecondaryConsoles_UsesEmptyArray()
    {
        // Arrange
        var primaryConsole = AnsiConsole.Create(new AnsiConsoleSettings());

        // Act
        var compositeConsole = new CompositeAnsiConsole(primaryConsole, null);

        // Assert
        Assert.NotNull(compositeConsole);
        // We can't directly access _consoles as it's private, so we'll test indirectly
        // by calling a method that uses _consoles and ensuring it doesn't throw
        Assert.Null(Record.Exception(() => compositeConsole.Write(new Text("Test"))));
    }
}